# Charts

Recharts-based, used lightly per Group 4 ("few high-quality charts > many useless charts").

- `AttendanceChart` — area/line attendance over time
- `GpaChart` — GPA trend
- `ScoreTrendChart` — final-score evolution

Every chart should: handle empty state, support filtering, and be responsive.
