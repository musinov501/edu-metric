# React 19 transition: many libraries still declare peer ranges for ^18.
# We opt into legacy peer-dep resolution at install time. Remove this once
# the dependency tree fully supports React 19 in its declared peer ranges.
legacy-peer-deps=true
